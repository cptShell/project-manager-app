export type ProfileLocalizationKey = {
    title: string,
    userData: {
        currentId: string,
        currentName: string,
        currentLogin: string,
    },
    buttons: {
        deleteUser: string,
    }
    editForm: {
        title: string,
        buttons: {
            editUser: string
        }
    }
};
  