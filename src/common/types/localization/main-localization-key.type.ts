export type MainLocalizationKey = {
  title: string,
  boardCreatingForm: {
    title: string
    inputs: {
      title: string,
      errors: {
        titleRequired: string,
      }
    }
    buttons: {
      createBoard: string
    }
  }
};
