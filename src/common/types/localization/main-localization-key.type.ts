export type MainLocalizationKey = {
  title: string,
  boardCreatingForm: {
    title: string
    inputs: {
      title: string,
    }
    buttons: {
      createBoard: string
    }
  }
};
