export type AuthLocalizationKey = {
  titles: {
    signIn: string;
    signUp: string;
  };
  inputs: {
    name: string;
    login: string;
    password: string;
  };
  buttons: {
    signIn: string;
    signUp: string;
  };
  messages: {
    dontHave: string;
    alreadyHave: string;
  };
};
